# Integration tests

Integration tests exercise real seams between the runner, a harness, a scenario, an experiment, storage, and external infrastructure.

Keep them deterministic and label any test that requires a local runtime.
