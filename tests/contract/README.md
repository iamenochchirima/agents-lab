# Contract tests

Contract tests verify that platform integrations, environment implementations,
harnesses, scenarios, experiments, telemetry emitters, and run stores satisfy the
laboratory's shared interfaces. Configuration tests verify that declared combinations
of harness variants, agent definitions, environments, and infrastructure are valid.

A contract should test observable behaviour, not a particular framework's internal design.
