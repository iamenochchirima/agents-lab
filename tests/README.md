# Tests

Repository-wide tests belong here when they exercise a shared interface or a cross-directory behaviour.

Keep implementation-specific tests close to their implementation. The contract suite is the main exception.
