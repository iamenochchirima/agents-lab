# Worker Crash experiment

Kill the worker during selected points in a run and measure recovery, lost state, and duplicate work.

This directory will hold the experiment protocol, deterministic fault plan, analyzers, and experiment tests.
