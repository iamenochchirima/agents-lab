# Run records

Generated run records are written here during local experiments.

A run should contain configuration, common events, trajectory, metrics, result, logs, and artifacts. Treat this directory as evidence, not source code.
