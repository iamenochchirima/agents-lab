# Remote computer environment

This environment gives the harness access to a remote machine or computer session.

Document connection behaviour, available interfaces, permissions, workspace lifetime, network access, machine state, isolation, and recovery after disconnects.
