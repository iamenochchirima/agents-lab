# API-native environment

This environment limits the harness to explicitly exposed tools and service interfaces rather than giving it general access to a computer or workspace.

Document the available tools, request and response shapes, authentication model, rate limits, failure behaviour, and test doubles.
