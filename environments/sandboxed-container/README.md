# Sandboxed container environment

This environment gives the harness an isolated process and filesystem space with explicit resource, network, and permission limits.

Document the container image, mounted data, available programs, resource limits, network policy, lifecycle, and cleanup procedure. Keep container hosting overhead distinct from harness behaviour when measuring runs.
