# Filesystem-native environment

This environment gives the harness a workspace of files and directories and may expose shell commands, programs, and persistent files as agent primitives.

Document workspace layout, permissions, process access, network policy, persistence, cleanup, and artifact ownership for each variant.
