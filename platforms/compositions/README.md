# Platform compositions

Platform compositions combine platforms or architectural layers used by the laboratory harness.

Use this directory when a comparison would be misleading if the combination were represented as only one platform.

Each composition may contain its own harness variants and platform-specific agent
definitions. It must state which platform owns each part of execution, state,
durability, retries, side effects, and telemetry.
