# Vercel AI SDK baseline harness variant

Placeholder for the first harness variant built with the Vercel AI SDK.

The baseline will use the SDK's native TypeScript and Node.js runtime and will expose
the common laboratory execution and telemetry contract through an explicit adapter.
