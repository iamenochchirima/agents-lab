# Standalone platform

Platform implementation in which the laboratory harness owns the main agent loop and lifecycle directly.

This platform may still use model APIs, databases, tools, and local services. Its defining property is that no external agent or workflow framework owns the main execution model.
