# Agentlab package

The agentlab package owns the concepts and behaviour shared by the laboratory.

Keep its interfaces small. Platform-specific behaviour belongs under the platforms directory or behind an explicit adapter seam.
