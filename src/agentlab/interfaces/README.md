# Interfaces

Defines the small interfaces that connect the laboratory runner to platforms, environments, telemetry, storage, and evaluation.

An interface includes its data shape, invariants, ordering rules, error modes, and lifecycle expectations.
