# Registry

Discovers available platform integrations, harness variants, agent definitions,
environment variants, scenarios, and experiments. It validates whether a requested
harness configuration names a supported combination and declares its infrastructure
requirements.

Registry entries should be explicit and inspectable. Avoid hidden import side effects.
