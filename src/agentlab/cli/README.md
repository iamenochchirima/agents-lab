# Command-line interface

Owns user-facing commands such as starting a run, comparing runs, listing implementations, and inspecting evidence.

Commands should delegate to deep laboratory modules rather than contain orchestration logic.
