# Laboratory source

This directory contains the generic laboratory control plane.

It coordinates experiments and stores evidence. It is not an agent framework and must not contain a platform-specific agent loop.
