# Adding an experiment

An experiment starts with a question. Record the hypothesis, controls, variables, procedure, fault plan, measurements, expected observations, and limitations before running it.

Make failures deterministic or seeded where possible. Record the configuration and injected failures in every run. Do not change a harness only to make it fit an experiment. If the experiment needs a capability a harness does not have, record that as a result.
