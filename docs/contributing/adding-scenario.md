# Adding a scenario

A scenario defines work an agent must accomplish. It should be useful for more than one harness.

Document the inputs, expected outputs, allowed tools, success criteria, fixtures, grading rules, and sources of ambiguity. Keep failure injection in experiments unless the failure is part of the workload itself.

A scenario is ready for comparison when another harness can execute it without changing its intent.
