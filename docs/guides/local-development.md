# Local development

This guide will document the commands needed to install dependencies, run the web application, run the laboratory CLI, start optional infrastructure, and execute the first deterministic test.

Keep required tools, service versions, environment variables, expected output, and cleanup steps explicit as the project gains implementation.
