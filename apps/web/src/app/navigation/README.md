# Application navigation

Navigation modules contain links and their active-state presentation. They do not
render page content or implement route transitions. Route paths are declared by the
router and consumed here through React Router links.
