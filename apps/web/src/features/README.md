# Features

Each feature represents a user-facing capability. Features may own views, data loading, and feature-specific presentation, but they should consume the published run data interfaces.

Platform and environment features are separate because a run can combine them.
