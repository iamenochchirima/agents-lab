# Platforms

Displays platform descriptors, variants, supported capabilities, required infrastructure, and implementation notes.

The UI should not assume that every platform supports the same capabilities.
