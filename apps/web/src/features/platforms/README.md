# Platforms

Displays platform descriptors, harness variants, platform-specific agent definitions,
compatible environments, required infrastructure, supported capabilities, and
implementation notes.

The UI should not assume that every platform supports the same capabilities. It should
show reusable environments and infrastructure beneath a harness variant as declared
relationships, not as resources owned by that platform.
