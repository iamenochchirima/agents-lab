# Repository map feature

The repository map displays the generated file tree and directory summaries. The
catalog generator reads the actual repository contents and nearby README files so the
view does not become a manually maintained copy of the directory structure.
