# Web tests

Contains frontend tests for routes, feature behaviour, rendering, and user-visible states.

Prefer tests that cross the feature's public interface rather than tests of implementation details.
