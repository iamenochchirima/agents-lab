# Web application

This will be the React and Vite interface for browsing runs, comparing results, inspecting telemetry, and viewing artifacts.

The web application should remain a consumer of laboratory data. It should not execute harness logic or become a second metrics implementation.
