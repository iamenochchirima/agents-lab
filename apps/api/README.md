# Laboratory API

This will be a thin read and control interface between the web application and the laboratory data.

It may expose run records, comparisons, descriptors, and artifacts. It must not absorb harness-specific execution logic.
