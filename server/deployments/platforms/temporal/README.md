# Temporal deployment profile

Temporal server, database, worker connectivity, persistence, and reset behavior belong
here. The Temporal agent implementation remains under `server/src/platforms/temporal/`.
