# Laboratory contracts

Versioned contracts are the only shared seam among the Lab server, backend platform
implementations, and external integrations. They define run requests, normalized
events, artifacts, and results—not agent-runtime internals.
