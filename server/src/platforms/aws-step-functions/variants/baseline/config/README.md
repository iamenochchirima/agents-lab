# config

The platform-level [`config.ts`](../../../config.ts) owns the effective profile. The
manifest records only its redacted projection; credentials and provider keys never enter
the run record.
