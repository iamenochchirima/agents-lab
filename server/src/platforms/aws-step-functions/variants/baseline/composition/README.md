# composition

Selects the baseline Activity and model capabilities. Broader skills, tools, OAuth
connections, plugins, policies, and artifacts remain outside this platform-specific
execution boundary until a concrete Step Functions variant needs them.
