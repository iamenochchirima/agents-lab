# state

Owns the mapping from Step Functions execution status and history to Lab inspection
records. The native reference retains the state-machine ARN, Activity ARN, execution
ARN/name, terminal status, retry count, and safe history metadata.
Owns this platform variant's state conventions.
