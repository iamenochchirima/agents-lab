# Temporal harness variants

Variants should make durable workflow structure and side-effect handling explicit.
The initial non-baseline variant uses OpenAI Agents SDK primitives inside Temporal;
the SDK is a variant choice, not a separate Lab platform.
