# Temporal with OpenAI Agents SDK

This planned variant uses OpenAI Agents SDK primitives inside a Temporal-hosted agent.
Temporal owns durable workflow execution; the SDK supplies the selected agent
primitives. Keep both layers visible in configuration, telemetry, and run evidence.
