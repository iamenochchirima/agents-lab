# Vercel Workflow / AI SDK platform

This directory will contain the Agent Harness Lab implementation using Vercel Workflow
execution with AI SDK primitives. Keep workflow durability, model/tool behaviour, and
native Vercel evidence explicit in each variant.
