# runtime

The baseline runtime is one Workflow invocation containing one model step. The native
Workflow run status is mapped to the Lab status; richer multi-turn loops are not yet
part of this bounded slice.
