# telemetry

The service returns native workflow/run identity and the workflow's normalized event,
trajectory, and metric records. The shared evidence writer is a primary integration
concern and is intentionally not modified by this platform-local slice.
