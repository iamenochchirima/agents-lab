# Http 429 experiment

Inject deterministic HTTP 429 responses and measure backoff, retry limits, and task outcome.

This directory will hold the experiment protocol, deterministic fault plan, analyzers, and experiment tests.
