# Tool Timeout experiment

Delay or time out a tool call and measure retry behaviour, cancellation, and continuation.

This directory will hold the experiment protocol, deterministic fault plan, analyzers, and experiment tests.
