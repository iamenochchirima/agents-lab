# Duplicate Event experiment tests

Tests the fault plan, controls, and expected evidence for the duplicate-event experiment.

Use deterministic fixtures and avoid relying on a live benchmark result.
