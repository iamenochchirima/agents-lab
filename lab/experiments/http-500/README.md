# Http 500 experiment

Inject deterministic HTTP 500 responses into an external dependency and measure recovery.

This directory will hold the experiment protocol, deterministic fault plan, analyzers, and experiment tests.
