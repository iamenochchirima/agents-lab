# Side Effect Ack Loss experiment tests

Tests the fault plan, controls, and expected evidence for the side-effect-ack-loss experiment.

Use deterministic fixtures and avoid relying on a live benchmark result.
